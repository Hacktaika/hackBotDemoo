# 🛡️ Система защиты от спама и DDoS

## Обзор

В проекте реализована комплексная система защиты от спама, флуда и DDoS-атак на всех уровнях приложения.

## Компоненты защиты

### 1. Rate Limiting (Ограничение частоты запросов)

**Файл:** `utils/rate_limit.py`

Система ограничения частоты запросов для разных типов действий:

- **Сообщения:** 15 сообщений в минуту (блокировка на 5 минут при превышении)
- **Callback queries:** 30 запросов в минуту (блокировка на 3 минуты)
- **Регистрация:** 3 попытки за 5 минут
- **Поиск по ключевым словам:** 10 запросов в минуту
- **Админ-панель:** 50 запросов в минуту (блокировка на 10 минут)
- **Рассылка:** 1 рассылка за 5 минут

### 2. Middleware для защиты

**Файл:** `middleware/rate_limit_middleware.py`

Автоматическая защита всех входящих сообщений и callback queries через middleware:
- Проверка rate limit перед обработкой
- Разные лимиты для админов и обычных пользователей
- Автоматическая блокировка при превышении лимитов

### 3. Валидация данных

**Файл:** `utils/validators.py`

- **Валидация размера сообщений:** максимум 4096 символов для текста, 1024 для caption
- **Валидация размера файлов:** максимум 50MB
- **Санитизация ввода:** очистка от опасных символов, ограничение длины
- **Проверка формата данных:** валидация перед сохранением в БД

### 4. Защита регистрации

**Файл:** `handlers/registration.py`

- Rate limiting: 3 попытки за 5 минут
- Валидация размера каждого поля:
  - Имя: 2-100 символов
  - Должность: 3-200 символов
  - Ожидания: 5-500 символов
- Проверка размера сообщений перед обработкой

### 5. Защита контент-хендлера

**Файл:** `handlers/content.py`

- Rate limiting для поиска по ключевым словам: 10 запросов в минуту
- Ограничение длины ключевого слова: максимум 100 символов
- Валидация размера сообщений
- Игнорирование незарегистрированных пользователей

### 6. Защита админ-панели

**Файлы:** `handlers/admin/*.py`

- Rate limiting для всех админ-действий
- Валидация размера контента при добавлении
- Ограничение размера файлов (50MB)
- Защита от перегрузки при массовых операциях

### 7. Защита рассылки

**Файл:** `handlers/admin/broadcast.py`

- Rate limiting: 1 рассылка за 5 минут
- Ограничение количества пользователей: максимум 1000 за раз
- Задержка между сообщениями: 50ms (20 сообщений/сек)
- Пауза каждые 20 сообщений: 1 секунда
- Автоматическая остановка при большом количестве ошибок (>50)

### 8. Защита от повторных запросов (Idempotency)

**Файл:** `utils/idempotency.py`

- Защита от дубликатов запросов
- Время жизни записи: 5 минут
- Используется для критических операций (проверка подписки и т.д.)

### 9. Защита базы данных

**Файл:** `database/db.py`

- Логирование медленных запросов (>1 секунды)
- Retry механизм при ошибках подключения (3 попытки)
- Экспоненциальная задержка при повторных попытках
- Pool pre-ping для проверки соединений

## Настройки защиты

### Лимиты для обычных пользователей:
- Сообщения: 15/минуту
- Callback queries: 30/минуту
- Регистрация: 3 попытки/5 минут
- Поиск по ключевым словам: 10/минуту

### Лимиты для админов:
- Сообщения: 15/минуту (с предупреждением)
- Callback queries: 30/минуту (с предупреждением)
- Админ-панель: 50/минуту
- Рассылка: 1/5 минут

### Блокировки:
- При превышении лимита сообщений: 5 минут
- При превышении лимита callback: 3 минуты
- При превышении лимита админ-панели: 10 минут

## Мониторинг

Все нарушения логируются с уровнем WARNING:
- Превышение rate limit
- Дубликаты запросов
- Медленные запросы к БД
- Ошибки валидации

## Рекомендации

1. **Мониторинг логов:** Регулярно проверяйте логи на наличие подозрительной активности
2. **Настройка лимитов:** При необходимости скорректируйте лимиты в `utils/rate_limit.py`
3. **Блокировка пользователей:** При постоянных нарушениях можно добавить постоянную блокировку в БД
4. **Расширение защиты:** Для продакшена рекомендуется добавить:
   - IP-based rate limiting
   - CAPTCHA для подозрительных пользователей
   - Анализ паттернов поведения
   - Интеграция с внешними сервисами защиты (Cloudflare и т.д.)

## Тестирование

Для проверки защиты:
1. Попробуйте отправить много сообщений подряд - должна сработать блокировка
2. Попробуйте быстро нажимать кнопки - должна сработать защита callback queries
3. Попробуйте зарегистрироваться несколько раз подряд - должна сработать защита регистрации

